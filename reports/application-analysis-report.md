# Application Analysis Report for agent01

## Overview
"agent01" is a small .NET console AI agent application designed to interact with the OpenAI API. It runs as an interactive console chat app and supports a limited set of local tools to operate within the folder where it is started.

## Key Features
- Interactive console chat application.
- Utilizes OpenAI .NET SDK for chat completions.
- Provides local tools for file system and git repository operations within its workspace root.
- Session transcript logging in Markdown format under `logs/` folder.

## Supported Local Tools
- GetWorkspaceRoot: Returns the current tool access root directory.
- GetFileInfo: Provides metadata for files/directories.
- GetGitStatus, GetGitDiff, GetRecentCommits, GetCurrentBranch: Git repository status and details.
- GetFileDiff: Difference of a file compared to HEAD.
- GetCommitDetails: Details for a specific commit.
- ListFiles, ReadFile, SearchText: File system operations within workspace root.
- ExportMarkdownReport: Creates or overwrites Markdown report files in workspace.
- StageFile, UnstageFile: Manage Git index staging.

## Workspace Root and Security
- The application restricts tool access to a configured workspace root directory.
- By default, the current working directory on app start is the workspace root.
- `AGENT_WORKSPACE_ROOT` environment variable can override the default root.
- Tools reject access to paths outside this root for security.

## Configuration
- .NET 10 SDK is required.
- Reads API key from `appsettings.local.json` or environment variable `OPENAI__APIKEY`.
- Uses layered configuration loading from `appsettings.json`, `appsettings.local.json`, and environment variables.

## Code Structure
- `Program.cs`: Main startup, configuration, chat loop including interaction with OpenAI and logging.
- `AgentTools.cs`: Definitions and implementations of the local tools, including workspace and Git commands.
- `TranscriptLogger.cs`: Handles per-session Markdown logging of user prompts, tool calls, assistant responses, system events, and errors.
- Configuration files for app settings and secrets (`appsettings.json`, `appsettings.local.json`).

## Additional Notes
- Transcript logs provide detailed interaction history useful for debugging and auditing.
- Git-related tools execute Git commands in the repository containing the workspace root with error handling for non-Git folders.
- The tool implementation emphasizes safe path resolution and access control to prevent unauthorized file system access.
- The application can be published and added to system PATH for running from any directory.

---
_Report generated by AI assistant_